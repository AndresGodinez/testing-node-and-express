{
  "name": "Gravty test",
  "description": "Gravty test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "nodemon src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^28.1.3",
    "supertest": "^6.2.4"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "transform": {
      "\\.js$": "<rootDir>/node_modules/babel-jest"
    }
  }
}
